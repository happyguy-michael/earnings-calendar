# Earnings Calendar - Product Roadmap

## Vision
Premium earnings calendar with AI-powered analysis, targeting retail traders who trade around earnings.

## Monetization
- **Broker affiliates** (primary) - Robinhood, Webull, Interactive Brokers
- Links on each ticker: "Trade $NVDA on Robinhood"

## Current State
- ✅ Basic calendar view (3-week rolling)
- ✅ Pre/post market grouping
- ✅ Beat/miss badges with surprise %
- ✅ Beat probability indicators
- ✅ Detail page with tabs (Overview/Analysis/History)
- ✅ EPS charts (line + bar)
- ✅ Mock historical data

## In Progress
- [ ] Premium UI polish (glassmorphism, gradients, glows)
- [ ] Mobile responsive design
- [ ] Real data integration

## Backlog - UI/UX
- [x] Animated number counters on stats ✓
- [x] Skeleton loading states ✓
- [x] Keyboard navigation ✓
- [x] Search/filter by ticker ✓
- [x] Smooth page transitions ✓
- [x] Quick filter chips (Beat/Miss/Pending) ✓
- [x] Dark/light mode toggle ✓
- [x] Mobile swipe navigation ✓
- [ ] Watchlist functionality
- [ ] Calendar export (Google/Apple)

## Backlog - Features
- [ ] Real earnings data API (Financial Modeling Prep / Alpha Vantage)
- [ ] AI-generated analysis (after earnings release)
- [ ] Price movement tracking (post-earnings)
- [ ] Options implied move display
- [ ] Sector/industry grouping
- [ ] Earnings call transcripts
- [ ] Analyst ratings integration

## Backlog - Technical
- [ ] PWA support
- [ ] Push notifications for watchlist
- [ ] Edge caching optimization
- [ ] Analytics tracking
- [ ] A/B testing framework

## Iteration Log
Track autonomous improvements here:

### 2026-02-26
- Initial setup
- EPS chart redesign (gradients, glows, premium styling)
- **Animated number counters** - CountUp component with cubic ease-out, staggered timing on stats cards and detail page EPS figures
- **Skeleton loading states** - Premium shimmer effect with linear gradients, staggered delays, full page skeletons for calendar + detail pages. Shows shaped placeholders during initial load for polished UX.
- **Keyboard navigation** - Arrow keys to navigate weeks, T for today

### 2026-02-27
- **Search/filter by ticker** - Glassmorphic search bar with animated focus states, keyboard shortcuts (/ to focus, Escape to clear), live filtering, result count indicator, and no-results empty state. Stats update dynamically based on filtered results. Mobile responsive.
- **Smooth page transitions** - Added template.tsx with fade/slide animations on navigation. Exit animation fades out with subtle upward movement, enter animation slides up with scale effect. Uses cubic-bezier easing for premium feel. CSS classes: `.page-transition`, `.page-exit`, `.page-enter`.
- **Hover tooltips** - Premium glassmorphic tooltips on earnings cards showing detailed EPS/revenue data. Displays actual vs estimate with surprise %, revenue figures, and beat probability explanations for pending earnings. Smooth fade-in animation, hidden on mobile for better UX.
- **Stat card hover micro-interactions** - Premium hover effects on stats cards: subtle lift (translateY + scale), animated gradient glow behind cards that pulses when hovered, color-matched glow variants for success/warning cards, cubic-bezier easing for smooth premium feel. Touch-friendly active state on mobile.
- **Quick filter chips** - Status filter toggles for All/Beat/Miss/Pending. Pill-shaped buttons with color-coded active states (green for Beat, red for Miss, amber for Pending). Shows count badges, applies to calendar view alongside search. Mobile-friendly with horizontal scroll. Stats and calendar update dynamically.
- **Dark/light mode toggle** - Animated sun/moon toggle switch with smooth theme transitions. Features include: bouncy thumb animation with spring easing, twinkling stars and moon craters on dark mode, rotating sun rays on light mode, full theme-aware CSS variables for all UI components (cards, tooltips, search bar, filters, stats, etc.), localStorage persistence, respects system preference on first visit. Light mode uses soft blue/white gradients with appropriate contrast adjustments.
- **Swipe gesture navigation** - Touch-friendly week navigation for mobile users. SwipeNavigator component detects horizontal swipes with intelligent vertical scroll protection. Features slide-in/out animations when navigating between weeks, a subtle hint banner shown until first swipe (persisted to localStorage), and works seamlessly alongside keyboard (←/→) and button navigation. Makes the calendar feel native on mobile devices.
- **Live reporting indicator** - Pulsing "LIVE" badge for stocks reporting today. LiveBadge component shows animated red pulsing indicator during active market windows (pre-market or after hours), and amber "Pre-Mkt"/"After Hrs" indicator before the window. LiveDot inline indicator shows a pulsing red dot next to ticker names for today's pending earnings. Adds visual excitement and urgency to the calendar. Integrated into both calendar cards and detail page.
- **Live countdown timer** - Real-time ticking countdown on calendar cards for today's pending earnings. CountdownBadge component shows "Reports in Xh Xm" that updates every second. Pulsing red animation when under 1 hour remaining, with urgent styling as release time approaches. Uses tabular-nums for stable width during countdown. Creates excitement and urgency for traders watching upcoming releases. Replaced static LiveBadge with dynamic countdown on main calendar view.
- **Smooth value transitions on CountUp** - Numbers now animate smoothly between values instead of resetting to 0. When filtering or navigating, stats cards show fluid transitions from current to new values. Dynamic duration based on value delta (faster for small changes). Tabular-nums for stable number widths during animation. Makes filter interactions feel premium and polished.

### 2026-02-28
- **Animated ProgressRing component** - Circular progress indicators now animate from 0 to their target value when the page loads or when scrolled into view. Uses Intersection Observer for performance (only animates when visible). Smooth ease-out-expo easing for premium deceleration feel. Staggered delays on earnings cards create a cascading fill effect. High-probability stocks (>70%) get a subtle pulsing glow overlay. Light mode support for track colors. Classic dashboard micro-interaction that makes the stats feel alive.
- **Animated sliding tab indicator** - Detail page tabs now feature a smooth sliding underline instead of static borders. When switching tabs, the gradient indicator (blue → purple with glow) slides smoothly to the new position using cubic-bezier easing. Subtle hover background on tab buttons. Focus-visible outline for accessibility. Light mode support. Respects prefers-reduced-motion. Classic premium UX pattern seen in Material Design and high-end finance dashboards.
- **Floating back-to-top button** - Premium FAB that appears when scrolling down 400px. Features gradient blue/purple background, pulsing glow effect, smooth spring-based entrance animation, arrow bounce on hover, and ripple effect on click. Respects prefers-reduced-motion. Common finance dashboard UX pattern that was missing.
- **Animated gradient border on hover** - Premium micro-interaction on earnings cards. When hovering, a rotating conic gradient flows around the card border using CSS `@property` for smooth angle animation. Features a blurred glow effect behind the border for depth. Today's pending earnings get an amber-themed variant that's always subtly animating. Light mode support with softer gradient colors. Common pattern in high-end financial dashboards.
- **3D tilt effect on stat cards** - Premium micro-interaction where stat cards subtly tilt based on mouse position with perspective transforms. Features: mouse-tracking rotation that creates a 3D depth effect, radial glare overlay following cursor position, spotlight effect with color variants per card type (blue default, green for success/beat rate, amber for pending). Respects prefers-reduced-motion and disables on touch devices. Creates a premium "Stripe dashboard" feel. Inspired by modern financial UI trends on Dribbble.

---
*This file is updated by the autonomous product iteration cron job.*

- **Chart refactor** - Completely redesigned EPS charts for readability. Replaced fancy vertical bars with clean horizontal bars showing actual vs estimate. Values are now large and inline (no squinting). Added table-style bar chart with clear columns. New sparkline trend component. New summary card showing beat rate, avg surprise, and EPS growth. Removed visual noise - focus on data, not decoration.
- **Navigation progress bar** - Premium loading indicator that appears during page navigation (YouTube/GitHub style). Features: gradient animated bar (blue → purple → pink), animated glow pulse at the leading edge, shimmer overlay effect, shadow depth beneath for dimension, intercepts internal link clicks to start animation, auto-completes when route changes, light mode support with softer gradients, respects prefers-reduced-motion. Common pattern in high-end finance dashboards that was missing.
- **EPS value labels on chart bars** - Added inline EPS values directly on the chart bars for better readability. Beat bars show green text, miss bars show red text. Values are positioned at the end of bars with proper spacing. Makes the chart instantly scannable without hovering.
- **Rolling number animation** - New NumberTicker component with slot-machine style digit-by-digit flip animation. RollingNumber component uses smooth vertical scroll transitions. Stats now animate dynamically when values change during navigation. Staggered delays create a cascading counter effect (40-50ms per digit). Respects prefers-reduced-motion. Replaces static CountUp for main stats. Creates a premium airport departure board feel.
- **Typewriter effect for AI Analysis** - AI analysis text now types out character by character with a blinking cursor, creating a ChatGPT-like experience. TypewriterParagraphs component handles multi-paragraph text with sequential animations. Includes animated TypingIndicator (bouncing dots) and Skip button for users who want to see full text immediately. Natural typing speed with slight variance for realistic feel. Premium UX pattern that makes the AI-generated content feel dynamic and live.
- **Animated flowing gradient text** - Header title gradient now flows smoothly through colors (blue → purple → pink → purple → blue) with a 4s animation cycle. Uses 200% background-size with background-position animation for smooth flow effect. Added `.text-gradient-static` variant for elements that shouldn't animate. Respects prefers-reduced-motion for accessibility. Subtle premium touch that catches the eye without being distracting.
- **Reading progress indicator** - Premium scroll progress bar for the AI Analysis tab. ReadingProgress component tracks scroll position through analysis content and shows a thin gradient progress bar at the top of the viewport. Features: gradient fill (blue → purple → pink) with leading glow effect, smooth transitions using requestAnimationFrame, visibility based on content being in viewport, optional percentage label. Common pattern on Medium/dev.to for long-form content. Makes reading AI analysis feel more engaging and provides clear progress feedback.
- **Material design ripple effect** - Click feedback animation on earnings cards. New Ripple component creates an expanding circular ripple from the click point (blue-tinted in dark mode, darker in light mode). Includes subtle press-down scale effect (0.985x) on :active state. Respects prefers-reduced-motion. Classic material design pattern that provides satisfying tactile feedback on interactions. Makes the calendar feel more responsive and premium.

### 2026-03-01
- **Shrinking sticky header on scroll** - Premium header that elegantly compresses when user scrolls past 60px. Features: reduced padding, hidden secondary elements (subtitle, keyboard hints, filter chips), smooth cubic-bezier transitions (0.35s), subtle shadow for depth when scrolled. Light mode support with softer shadows. Respects prefers-reduced-motion. Mobile-aware: keeps filter chips visible even when scrolled. Classic pattern seen on Linear.app, Vercel.com, and premium finance dashboards. Creates more content space while maintaining navigation access.
- **Scroll-triggered staggered reveal animations** - New ScrollReveal system with multiple animation types (fade-up/down/left/right, scale, blur). Uses Intersection Observer for performant viewport detection. StaggeredReveal applies cascading delays to child elements. RevealTableBody specialized for table rows. Applied to History tab where cards fade in on scroll and table rows slide in from left with staggered timing. Analysis tab key takeaways cascade in one by one. Respects prefers-reduced-motion. Classic premium pattern (Stripe, Linear) that makes content feel alive as user scrolls.
- **Animated aurora glow effect on today's column** - Subtle breathing aurora animation on today's date header that makes it immediately identifiable. Features: animated gradient shift (6s cycle) that flows through blue/purple tones, moving glow orb with 8s cycle that creates depth and movement, extended subtle effect to today's content column for cohesive visual treatment. Full light mode support with softer, cooler tones. Respects prefers-reduced-motion. Modern 2025 dashboard trend that draws attention without being distracting.
- **Premium shimmer sweep effect** - Holographic light sweep animation on earnings cards when hovered. Features: diagonal gradient that slides across the card from left to right (0.8s duration), skewed angle for natural light reflection feel, subtle white gradient overlay, light mode support with increased opacity, respects prefers-reduced-motion. Classic premium UI pattern seen on credit cards and high-end SaaS dashboards. Adds satisfying visual feedback to card interactions.
- **Cursor spotlight ambient glow** - Subtle radial gradient spotlight that follows the mouse cursor across the page, adding a "living" quality like on Linear.app or Vercel.com. Features: smooth easing for natural movement (doesn't snap to cursor), brand-matched colors (blue/purple gradient), theme-aware with softer colors in light mode, fades in/out when cursor enters/leaves viewport, performant using requestAnimationFrame and CSS transforms, respects prefers-reduced-motion. Creates an ambient interactive layer that makes the entire page feel responsive and premium.
- **Premium animated focus states** - Elevated keyboard navigation experience with animated gradient focus rings. Features: rotating conic gradient border (same as hover effect) activates on focus-visible, pulsing glow animation (breathing effect), button focus with animated box-shadow that cycles through brand colors, card focus-within detection for nested focusable elements, amber-themed focus for today's pending earnings. Includes skip-to-main accessibility link that reveals on Tab press (hidden until focused), semantic `<main>` element wrapper. Light mode support with softer glows. Full prefers-reduced-motion support with static fallbacks. Improves WCAG compliance while maintaining premium visual polish.
- **Magnetic button effect** - Premium micro-interaction where navigation buttons "attract" toward the cursor when it approaches (Linear.app/Apple.com style). MagneticButton component applied to Today button and week navigation arrows. Features: configurable intensity and radius, smooth cubic-bezier easing for natural movement, subtle glow effect when cursor is in magnetic field, scale up when close, respects prefers-reduced-motion, light mode support, performance optimized with requestAnimationFrame. Creates an alive, responsive feel to interactive elements.
- **Animated typewriter search placeholder** - Search bar now features a dynamic placeholder that cycles through example search terms (NVDA, Apple, Tesla, Microsoft, etc.) with a typewriter effect. Creates visual interest while also demonstrating searchable terms to users. Features: realistic typing/deleting animation with cursor, configurable timing (70ms type, 35ms delete, 2.5s pause), respects prefers-reduced-motion with static fallback, reusable useAnimatedPlaceholder hook for customization. Inspired by Algolia's animated placeholder pattern. Subtle UX enhancement that makes the search bar feel alive and guides users on what to search.
- **Elastic spring button press animation** - All buttons now have a satisfying, bouncy press animation using CSS `linear()` timing function for realistic spring physics. Features: buttons squish down to 92% on press, spring back with slight overshoot on release, uses carefully tuned spring parameters (stiffness 400, damping 15), applied to all interactive elements (nav buttons, filter chips, theme toggle, back-to-top), softer bounce variants for smaller/larger elements, keyboard focus pulse animation, respects prefers-reduced-motion with static fallback. Inspired by native iOS/macOS button feel. Makes every click feel tactile and premium.
- **Staggered blur fade-in tooltips** - Tooltips now fade in with a blurred-to-sharp transition for a premium glass effect.

### 2026-03-02
- **Animated SVG market session icons** - Replaced emoji (☀️🌙) with custom animated SVG icons for pre-market and after-hours sessions. Sun icon features: rotating rays (20s cycle) with individual pulse effects, glowing core that breathes, radial glow background. Moon icon features: twinkling stars with staggered delays, sparkle crosshairs that rotate, glowing crescent shape, small crater details. Icons speed up rotation on hover for interactivity. Full light/dark mode support with softer colors in light mode. Respects prefers-reduced-motion. Adds distinctive visual character to the calendar while maintaining clear session identification.
- **Keyboard shortcuts help overlay** - Press `?` to toggle a modal showing all available keyboard shortcuts, organized by category (Navigation, Search, General). Features: spring-animated modal entrance with staggered reveal of shortcut groups and rows, keyboard icon button in header that pulses on first visit to draw attention, fully themed for light/dark mode, glassmorphic styling matching the app design, Escape to close, click-outside to dismiss. Classic power-user feature seen on GitHub, Notion, Linear. Improves discoverability of keyboard navigation features.
- **Animated empty state component** - Days with no earnings now show themed animated icons instead of plain text. Past days show a green checkmark with confetti particles, today shows a pulsing dot, future days show an animated clock. Icons appear with staggered entrance animation and reveal sublabels on hover.
- **Sparkle particle effect on beat badges** - Beat badges now have subtle particles emanating outward, creating a continuous celebration effect. Features: 6 particles per badge bursting in a radial pattern, green/lime/gold color palette matching success theme, particles regenerate every 3s for continuous animation, BadgeStarBurst variant also available with 4-pointed stars, light mode support with enhanced drop shadows, respects prefers-reduced-motion. Adds visual reward for positive earnings surprises.
- **Sliding pill indicator on filter chips** - Filter buttons now share a unified animated pill that glides smoothly between selections. Features: spring-based cubic-bezier easing (0.34, 1.56, 0.64, 1) for bouncy, premium feel, color-matched pill with glow for each filter type (green for Beat, red for Miss, amber for Pending), subtle inner glow on colored variants, individual button backgrounds removed in favor of unified sliding indicator. Light mode support with softer shadows. Respects prefers-reduced-motion. Classic iOS segmented control / Linear.app pattern that creates a more connected, fluid interaction.
- **Animated mesh gradient background** - Premium ambient background with 5 softly drifting color blobs that blend together to create a living mesh gradient effect. Features: organic drifting movement with varying speeds (20-28s cycles), gaussian blur for soft edges and seamless blending, colors include blue, purple, pink, teal, and indigo that overlap and merge, theme-aware opacity (60% dark / 40% light) to not distract from content, hardware-accelerated transforms, mobile optimized (smaller blobs, reduced blur for performance), respects prefers-reduced-motion. Inspired by Linear.app, Vercel.com, and Stripe.com dashboard backgrounds. Adds a premium "living" quality to the page without being distracting.
- **Animated share menu with social icons** - Share button now opens an animated dropdown menu with X/Twitter, LinkedIn, Reddit share options plus copy-to-clipboard. Features: spring-based menu entrance animation (scale + translate), staggered slide-in for each menu item, icon scale on hover, animated SVG checkmark stroke draw when link is copied, auto-close after copy with success state. Full keyboard support (Escape to close), click-outside dismissal, respects prefers-reduced-motion. Share text is context-aware with beat/miss/met emojis and surprise percentage. Makes sharing results frictionless and delightful.
- **Animated path-drawing EPS trend sparkline** - New AnimatedSparkline component with SVG stroke-dasharray animation. Features: line draws itself when scrolled into view using Intersection Observer, gradient area fill that fades in after line draws, data point dots that pop in with bouncy spring animation, pulsing highlight on the latest value. EPSTrendSparkline variant displays 4-quarter EPS history with trend indicator and YoY growth percentage. Uses cardinal spline interpolation for smooth curves. Beat/miss quarters are color-coded (green dots for beats, red for misses). Added to detail page sidebar for quick visual trend assessment. Full light/dark mode support, respects prefers-reduced-motion. Classic premium visualization pattern seen on Stripe and Linear dashboards.
- **Animated toast notification system** - New ToastProvider context with premium glassmorphic toasts. Features: slide-in/out animations with cubic-bezier easing, type variants (success/info/warning/error) with color-coded left accent bars, animated icon ring pulse on appearance, auto-dismiss progress bar, dismiss button with hover effects, stacked toast support. Integrated with theme toggle (shows "Dark/Light mode enabled" feedback) and keyboard shortcut T (shows "Jumped to current week" feedback). Full light/dark mode support with appropriate shadows, respects prefers-reduced-motion. Classic UX pattern seen in Stripe, Vercel, and Linear dashboards for action feedback.
- **Animated beat streak indicator** - New BeatStreak component showing consecutive earnings beats with animated flame icon. Features: three streak levels (warm 3+, hot 5+, legendary 8+), animated flame SVG with flicker effect, ember particles that rise and fade for high streaks, rainbow flame animation for legendary status (8+ beats), count badge with glow effects. BeatStreakBadge inline variant for compact display on calendar cards. Integrated into both main calendar view and detail page header. Full beatStreaks data map with 30+ companies and their historical consecutive beat counts (e.g., NVDA: 12, CRWD: 9, AVGO: 10). Full light/dark mode support, respects prefers-reduced-motion. Inspired by gaming streak indicators and Dribbble finance dashboards. Adds visual reward for consistent earnings performers.

### 2026-03-03
- **Premium shine sweep animation on beat badges** - Periodic diagonal light sweep effect on beat badges (credit card holographic style). Features: subtle white gradient bar that sweeps across the badge every 5 seconds, skewed transform for natural light reflection angle, smooth ease-in-out timing with delayed start, light mode support with slightly stronger shine intensity for visibility. Respects prefers-reduced-motion (animation disabled). Classic premium UI pattern seen on credit cards and high-end SaaS products. Makes successful earnings feel extra special and visually rewarding.
- **Value change highlight pulse on stats** - Stats cards now pulse with color-matched glow when values change during filtering. ValueChangeHighlight component wraps any content and triggers a scale+glow animation when the tracked value changes. Features: smooth 600ms animation with cubic-bezier spring easing, color variants (blue default, green success, amber warning, red danger), brief scale to 1.05x with expanding glow ring and shadow, light mode support with softer colors, skipInitial option to avoid flash on page load, respects prefers-reduced-motion (fade-only fallback). Also includes SmartValueHighlight variant that auto-detects increase/decrease and applies success/danger colors accordingly. Common pattern in trading/finance dashboards that draws the user's eye to changed values. Makes filtering interactions feel more responsive and premium.
- **Blur-to-focus entrance animations** - Premium 2025 micro-interaction trend where elements appear to "come into focus" rather than just sliding in. Features: earnings cards now include 4px→0px blur in entrance animation with optimized cubic-bezier easing (0.22, 1, 0.36, 1), week navigation slides have subtle 3px blur effect, stat cards get staggered blur-to-focus animation (8px→0px) with cascading 80ms delays. Uses will-change hint for GPU acceleration. Respects prefers-reduced-motion with simple fade fallback. Inspired by Linear.app, Vercel.com, and premium finance dashboards. Creates a sophisticated, high-end feel as content enters the viewport.
- **Animated pulsing legend indicators** - Legend dots at the bottom now pulse with theme-matched animations. Beat indicator has confident green radial pulse, miss indicator has subtle red pulse, pending/odds indicator has amber breathing glow. New LegendProgressRing animates fill when scrolled into view with ambient glow effect. Intersection Observer triggers entrance animation. Full light/dark mode support, respects prefers-reduced-motion. Makes the legend feel alive and consistent with the rest of the polished UI.
- **Countdown progress ring with visual time depletion** - CountdownBadge now features an animated SVG progress ring that visually depletes as time passes. Ring shows 100% for >1 hour remaining and smoothly depletes to 0%. Color transitions from blue (plenty of time) → amber (under 15 min) → red (under ~6 min). Features: gradient stroke for premium glass effect, pulsing glow filter when urgent (<5 min), centered timer icon within ring, smooth 1s linear transitions for second-by-second updates. Full light/dark mode support with appropriate track colors. Respects prefers-reduced-motion. Mobile responsive sizing. Premium finance dashboard pattern seen on Tradingview, Bloomberg Terminal, and high-end trading apps that makes pending earnings countdowns more visually engaging and immediately scannable.

### 2026-03-03
- **Animated checkmark/X icons for beat/miss badges** - Result status badges now feature animated SVG icons that draw themselves on page load. Features: checkmark draws itself with satisfying stroke animation using stroke-dasharray/offset technique for BEAT results, X icon animates in with sequential line draws followed by subtle shake effect for MISS results. New AnimatedBadgeIcon component uses Intersection Observer to trigger animation when visible. Circle background pops in with spring easing (cubic-bezier 0.34, 1.56, 0.64, 1). Full prefers-reduced-motion support with instant reveal fallback. Light mode color adjustments (green-600, red-600). Applied to main status badge on report detail page hero section. Classic premium pattern seen on Stripe, Linear, and modern fintech dashboards that provides satisfying visual feedback for earnings results.
- **Premium logo shine sweep on card hover** - Company logos on earnings cards now feature a holographic-style shine sweep animation when hovered. Features: diagonal light gradient that sweeps across the logo container (0.6s duration), skewed transform for natural light reflection angle, subtle blue glow and border highlight on the container during hover. Light mode support with brighter shine intensity for visibility on lighter backgrounds. Respects prefers-reduced-motion. Classic premium pattern seen on credit cards and high-end product displays that adds polish to card interactions.
- **Spring physics timing functions for micro-interactions** - Major animation system upgrade using CSS `linear()` timing function to implement true physics-based spring animations. Four reusable spring presets as CSS custom properties: `--spring-snappy` (subtle bounce for buttons/quick interactions), `--spring-bouncy` (noticeable bounce for card entrances), `--spring-elastic` (high energy for special emphasis), `--spring-smooth` (gentle settle without overshoot). Applied to: stat card hover/entrance animations, earnings card hover with separated transform properties, filter chip selections, back-to-top button entrance (elastic spring), week slide navigation (bouncy spring with 40px travel), rolling number digit flip animations, search clear button scale. Inspired by Josh Comeau's linear() timing function research. Creates a premium, alive feel similar to Linear.app and Stripe dashboards where interactions have natural physics-based movement rather than mechanical easing curves.
- **Search suggestions dropdown with keyboard navigation** - Search bar now shows a dropdown of matching tickers as you type. Features: fuzzy matching on ticker symbol and company name (min 1 char), up to 8 suggestions sorted with ticker matches first, keyboard navigation (↑↓ to navigate, Enter to select, Escape to close), highlighted matching text with blue accent color, market session icons (☀️/🌙), status dots (green for reported, amber pulse for pending). Spring-animated entrance (0.2s) with translateY + scale, glassmorphic background with backdrop blur, proper ARIA roles for accessibility (combobox, listbox, option). Mobile-friendly: fixed bottom sheet layout on small screens. Light mode support with softer colors. Keyboard hints displayed at bottom of dropdown. Click-outside to close. Classic UX pattern seen on Algolia, GitHub, and Linear search boxes that enables faster navigation to specific tickers without scrolling through the calendar.
- **Premium animated 404 page with glitch effects** - Custom not-found page with creative financial theme. Features: glitchy "404" text with random color shift bursts using clip-path animations, animated broken stock chart SVG with line drawing animation and red X break point, floating ticker symbols ($LOST, $GONE, $NULL, $VOID, etc.) drifting down the page, CRT-style scan line overlay, ambient red/orange gradient glow, premium CTA button with magnetic effect. Error code displays "TICKER_NOT_FOUND" for thematic consistency. Full light/dark mode support, respects prefers-reduced-motion. Inspired by creative 404 pages on Vercel and Linear. Turns error state into a polished, on-brand experience.

### 2026-03-04
- **Animated TimeSince badges for recently reported earnings** - New TimeSinceBadge component shows "Reported Xh ago" for recently released earnings (within 7 days). Features: pulsing green "NEW" badge with expanding ring animation for very recent results (<30 min), gradient glow effect for recent results (<4 hours), real-time updating every minute, inline TimeSinceInline variant for compact card display, green dot indicator with pulse animation. Integrated into detail page report info card and main calendar cards (for today's reported stocks). Full light/dark mode support with appropriate color adjustments. Mobile responsive with smaller sizing. Respects prefers-reduced-motion. Provides temporal context for traders to identify fresh earnings releases at a glance. Common pattern in news feeds and trading platforms that highlights recency.
- **Parallax floating effect on stat cards** - Premium micro-interaction where stat cards subtly float at different rates as the user scrolls, creating a layered, depth-rich feel. Features: scroll-based parallax movement with configurable intensity (3.5%–5% per card), staggered activation delays (0ms → 150ms) for organic cascading effect, varied intensities per card for natural look, performance optimized with requestAnimationFrame, mobile-friendly with reduced intensity on touch devices. Full prefers-reduced-motion support. Cubic-bezier easing (0.22, 1, 0.36, 1) for smooth, premium movement. Inspired by Linear.app, Vercel.com, and premium finance dashboards. Creates subtle depth without being distracting.
- **Live notification pulse on Today button** - TodayButton component with pulsing notification dot when pending earnings exist for today. Features: animated red/orange gradient dot with expanding ping animation (similar to Tailwind's ping utility), count badge appears on hover showing number of pending reports, subtle glow animation on the button itself when live earnings exist, tooltip on hover with pending count, full theme-aware styling (dark/light mode), respects prefers-reduced-motion. Creates visual urgency and draws attention to live action, common pattern in trading platforms for real-time notifications.
- **Animated session dividers with flowing gradients** - New SessionDivider component adds premium animated gradient lines between Pre-Market and After Hours sections. Features: flowing gradient animation (blue/purple for pre-market, amber/orange for after-hours), shimmer overlay effect that sweeps across the line, pulsing end caps with glow effects, Intersection Observer for scroll-triggered entrance animation (scale from center), blurred glow layer underneath for depth. Full light/dark mode support with softer glow in light theme. Respects prefers-reduced-motion with static fallback. Adds clear visual hierarchy between market sessions, making the calendar instantly scannable. Common pattern in premium finance dashboards for section separation.
- **Animated Market Status indicator** - New MarketStatus component showing live market session status in the header. Features: real-time display of current market phase (Pre-Market, Market Open, After Hours, Closed), animated countdown to next market event (e.g., "Market opens in 2h 15m"), progress bar showing session completion during active trading hours, phase-specific color coding (amber for pre-market, green for regular hours, purple for after-hours, red for closed), pulsing status dot with animated glow during open sessions. Full light/dark mode support with appropriate gradient backgrounds. Mobile responsive with compact view on small screens. Respects prefers-reduced-motion. Practical feature for traders to know market timing at a glance. Integrated into main header next to title.
- **Animated stock ticker ribbon** - Premium scrolling stock ticker tape (Bloomberg/CNBC style) showing recent earnings results and today's pending reports. Features: smooth infinite CSS scroll animation with GPU acceleration, company logos with fallback initials, beat/miss indicators with color-coded arrows and surprise percentages, pending earnings with pulsing odds indicators, pause on hover with visual pause indicator overlay, gradient fade edges for seamless look, duplicate content for seamless loop, click-through to individual report pages. Full light/dark mode support with appropriate background gradients. Mobile responsive with smaller sizing. Respects prefers-reduced-motion (animation disabled). Creates visual activity at the top of the page and quickly surfaces recent results without user scrolling. Common pattern in finance dashboards and trading platforms.
- **Animated stat icons with entrance animations** - New AnimatedStatIcon component adds themed SVG icons to each stat card. Features: Total Reports gets stacked bar chart icon with sequential rise animation and ambient glow pulse, Beat Rate keeps its progress ring with refined layout, Reported gets clipboard icon with self-drawing checkmark (stroke-dasharray animation) and success glow pulse, Pending gets hourglass icon with continuous sand fall animation and breathing glow. All icons animate on scroll into view using Intersection Observer. Entrance animations use spring physics timing functions (--spring-bouncy) for natural movement. Icons provide visual differentiation between metrics, making the stats row instantly scannable. Full light/dark mode support with appropriate color adjustments. Full prefers-reduced-motion support with instant reveal fallback. Classic dashboard pattern seen on Stripe, Linear, and premium finance platforms where icons add visual hierarchy to metric cards.
- **Badge glow intensification on hover** - Enhanced micro-interaction where beat/miss badges glow more intensely when hovering over earnings cards. Features: subtle scale-up (1.05x) on hover for satisfying pop, multi-layer box-shadow with inner glow for depth (25px outer + 50px diffuse + 15px inner glow), increased background opacity and brighter border on hover, green glow for beats, red glow for misses, spring-snappy easing for smooth transitions. Full light mode support with softer glow intensity. Full prefers-reduced-motion support (no scale). Makes the result badges feel more impactful and draws attention to the key metric on each card.

### 2026-03-05
- **Animated comparison bars in earnings tooltip** - Premium EPS/Revenue visualization in hover tooltips. Features: animated horizontal bars showing actual vs estimate with staggered grow animation using spring-bouncy easing (0.34, 1.56, 0.64, 1), dashed outline for estimate bar vs solid gradient fill for actual, color-coded beat (green) vs miss (red) with matching glow effects, spring-animated delta badge popping in with surprise percentage, value labels animating in after bars complete. Full light/dark mode support with appropriate gradient/glow intensities. Full prefers-reduced-motion support (instant reveal fallback). Inspired by Bloomberg/Refinitiv dashboard patterns. Makes the surprise immediately visible at a glance, improving information density without requiring click-through.
- **Animated flip digit countdown timer** - Airport departure board style countdown for pending earnings. New FlipDigit component with 3D CSS transforms and perspective. Features: cascading flip delays (0ms → 150ms stagger) for natural movement, top/bottom panel flip animation using rotateX transforms, FlipClock component for hours:minutes:seconds with pulsing colon separators, FlipCountdownBadge replacing simple text countdown, "REPORTING NOW" state with pulsing ring animation, monospace digit rendering with tabular-nums for stable width. Full light mode support with inverted panel colors. Respects prefers-reduced-motion (hides flip panels, shows static digits). Inspired by airport departure boards and casino slot machines. Creates urgency and excitement for traders watching pending earnings releases. Common pattern in countdown UIs like Kickstarter and Apple product launches.
- **Dynamic tab title with pending count** - New DynamicTitle component that updates the browser tab title based on pending earnings today. Features: shows count in title like "(3) 📊 Earnings Calendar" when there are pending earnings, alternating title animation when tab is in background for subtle attention-grabbing without being annoying, restores clean title when no pending earnings. Helps traders stay informed even with multiple tabs open — common UX pattern in trading platforms and notification-heavy apps like Slack/Discord. Zero visual footprint (component renders nothing).
- **Wave reveal animation for week navigation** - Premium cascading entrance animation when navigating between weeks. Features: day headers animate in with staggered 50ms delays (dayHeaderWaveIn), day content columns animate with bouncy spring physics at 65ms intervals offset by 30ms from headers, subtle blue ripple glow effect that expands and fades on each column during entrance, wave flows left-to-right for natural reading direction feel. Creates a sophisticated "domino effect" that makes week navigation feel fluid and responsive. Full light/dark mode support with softer ripple in light theme. Full prefers-reduced-motion support (instant reveal, no animation). Inspired by Linear.app and Vercel dashboard transitions. Common premium pattern that adds polish to navigation without impacting performance.
- **Animated copy-to-clipboard button for tickers** - Premium CopyTicker component for quick ticker symbol copying. Features: click to copy ticker to clipboard, animated icon transition (copy → checkmark → copy) with stroke-dasharray draw effect, success bounce animation using spring-snappy physics, radial glow pulse that expands and fades on successful copy, CopyTickerInline variant for clickable ticker text with tooltip. Integrated into detail page header and hover tooltips. Full light/dark mode support with appropriate color adjustments. Respects prefers-reduced-motion. Useful for traders who frequently copy symbols to paste into trading platforms, screeners, or research tools. Common pattern in Bloomberg Terminal and fintech dashboards.
- **Animated semi-circular gauge for beat odds** - Replaces the simple progress ring + text combo with a premium speedometer-style gauge visualization. OddsGauge component features: 180° arc that fills from left to right based on probability, gradient color (red → amber → green) matching the value, animated needle indicator that follows the fill, tick marks at 0%/25%/50%/75%/100% for visual reference, glow effect on high probability gauges (>70%), Intersection Observer for scroll-triggered animation, spring-like easing for natural movement. Full light/dark mode support with appropriate track colors. Respects prefers-reduced-motion. Inspired by trading dashboard speedometers and Bloomberg-style probability meters. Makes beat probability immediately scannable and more visually engaging than a simple ring.
- **Cursor-following border glow spotlight** - Premium micro-interaction where a localized radial glow follows the cursor position along the card border. BorderGlowSpot component creates a focused spotlight effect at the exact mouse position, different from the rotating gradient border - this tracks real-time cursor movement. Features: radial gradient centered on cursor with soft falloff, outer diffuse glow layer with blur for depth and dimension, color variants (default blue, success green, warning amber, danger red), theme-aware intensity (softer in light mode), subtle pulse animation on hover. Applied to week calendar cards. Full light/dark mode support. Respects prefers-reduced-motion. Also includes useBorderGlowSpot hook for custom implementations. Inspired by Linear.app and Stripe dashboard hover states where cards feel alive and responsive to cursor movement.

### 2026-03-06
- **Imminent alert pulsing halo effect** - New ImminentGlow component adds visual urgency to earnings cards reporting within 15 minutes. Features: amber breathing glow when within 15 minutes, red pulsing glow when within 5 minutes, intensity scales with urgency (closer = stronger), dual-layer effect with outer shadow glow + inner gradient border ring, smooth CSS animations with breathing and pulse keyframes, hooks (useImminentState) for conditional styling elsewhere. Full light/dark mode support with adjusted glow intensities. Full prefers-reduced-motion support with static border fallback. Complements FlipCountdownBadge by adding ambient visual cues that make imminent reports stand out from the rest of the calendar. Common pattern in trading platforms like Bloomberg Terminal and TradingView where time-sensitive items get ambient highlighting.
- **Premium broker affiliate CTA buttons** - New BrokerCTA component for monetization via broker affiliate links. Features: gradient shimmer sweep on hover (holographic credit card effect), magnetic pull effect toward cursor position, spring-bounce press animation with scale feedback, broker logos with fallback initials, pulsing glow on hover for emphasis, staggered entrance animations with configurable delays. Supports major brokers: Robinhood, Webull, Interactive Brokers, Schwab, Fidelity, E*TRADE. Includes BrokerCTAGroup component for displaying multiple broker options. Full light/dark mode support with appropriate shadows and colors. Full prefers-reduced-motion support. Integrated into report detail page sidebar as "Trade $TICKER" section with affiliate disclosure. Common monetization pattern in financial dashboards (Yahoo Finance, Benzinga, etc.) that adds revenue potential while providing user value.
- **Animated dot grid background with cursor glow** - New AnimatedGridBackground component adds a premium ambient background layer. Features: subtle dot grid pattern with configurable gap and size (40px gap, 1.2px dots), diagonal pulsing wave animation that flows across the grid (5s cycle), cursor-following spotlight effect where dots brighten and grow slightly near the mouse position (180px radius, 0.4 intensity), canvas-based rendering for performance, theme-aware colors (white dots on dark, black dots on light mode). Includes StaticDotGrid CSS-only fallback and GridMask component for edge fading. Full prefers-reduced-motion support with static rendering. Inspired by Linear.app, Vercel.com, and Stripe's background grids that add depth and a tech-forward aesthetic without distracting from content. Creates a subtle "data visualization" feel appropriate for a financial calendar. Layered behind FloatingParticles and GrainOverlay for complete ambient effect.
- **Animated surprise percentage reveal on badges** - New SurpriseCountUp component adds counting number animation to beat/miss badges. Features: numbers count up from 0 to actual surprise value when scrolled into view, ease-out-expo easing for smooth deceleration, staggered delays that cascade down the earnings list (200ms base + 50ms per card), tabular-nums for stable width during animation, AnimatedSurpriseBadge full variant with spring-bouncy entrance (scale 0.6→1.08→1) and completion glow pulse, Intersection Observer for viewport-triggered animation. Creates a satisfying "reveal" moment when viewing earnings results - makes the surprise feel more impactful. Full prefers-reduced-motion support with instant display fallback. Inspired by trading terminal number reveals and fintech dashboard animations where dynamic data deserves dynamic presentation.
- **Enhanced search empty state with animated illustration** - New SearchEmptyState component provides premium UX when search/filter returns no results. Features: animated magnifying glass with self-drawing SVG strokes (stroke-dasharray animation), scanning line animation inside the search circle, expanding pulse rings with staggered timing, fuzzy ticker suggestions based on search query (prefix matching, character overlap scoring), floating background orbs with parallax movement (3 orbs with independent animation cycles), clear action buttons (Clear search, Clear filters) with gradient primary styling, staggered entrance animations for all content elements (icon → text → suggestions → actions). Full light/dark mode support with adjusted orb opacities and button colors. Full prefers-reduced-motion support (instant reveal, no animations). Improves the "no results" experience by providing visual feedback, suggested alternatives, and quick actions - turning a dead-end into a helpful interaction. Common premium pattern in apps like Notion, Linear, and Slack where empty states are treated as first-class UI experiences.
- **DisasterMiss component for significant earnings misses** - New DisasterMiss component provides visual symmetry as the counterpart to ExceptionalGlow. Features: red pulsing ring effect that radiates outward for significant misses (>-15% surprise), enhanced "catastrophic" mode for massive misses (>-25%) with additional crack overlay SVG animation, DisasterMissIcon (📉) with subtle shake animation for immediate visual identification, Intersection Observer for viewport-triggered animation, SeverityIndicator convenience wrapper that handles both beats and misses. Full light/dark mode support with appropriate red color intensities. Full prefers-reduced-motion support with static glow fallback. Useful for traders watching for overreaction buy opportunities where big misses can present value entries. Common pattern in premium trading dashboards where both positive and negative outcomes get distinctive visual treatment.
- **Premium hover micro-interactions on ticker ribbon** - Enhanced ticker ribbon items with liquid-design inspired hover effects. Features: subtle scale-up (1.02x) and lift (2px translateY) using spring-snappy physics, animated gradient underline that slides in from center (blue→purple gradient, 80% width), ambient radial glow behind hovered items (indigo tinted), result badge "pop" animation (1.1x scale) with color-matched text glow (green for beats, red for misses), directional arrow bounce (up for beats, down for misses) using spring-elastic timing, logo scale-up (1.15x) with glow effect, ticker symbol color accent to blue on hover. Full light/dark mode support with softer glow intensities in light theme. Full prefers-reduced-motion support (all transforms disabled, only underline remains). Inspired by 2026 UI trends combining glassmorphism with liquid-like interactions - makes the ticker ribbon feel alive and responsive to cursor movement. Common premium pattern in finance dashboards where data elements provide rich hover feedback.

### 2026-03-07 — 02:53
- **Hover focus mode** - When hovering an earnings card, sibling cards subtly dim (55% opacity) and blur (0.8px), creating a spotlight effect that draws attention to the hovered card. Features: spring-physics transform with subtle lift and scale (1.01x, -2px), smooth cubic-bezier transitions (0.22, 1, 0.36, 1), light mode support with softer dimming (50% opacity), full prefers-reduced-motion support (opacity only, no blur/transform). Premium pattern from Linear.app and Stripe dashboards that reduces visual noise and improves card scanability. CSS-only implementation using `:has()` selector — no JavaScript required.
- **Filter-colored ambient glow** - New FilterGlow component adds a subtle ambient glow to the calendar container when a non-"all" filter is active. Features: color-matched radial gradients (green for beats, red for misses, amber for pending), soft breathing animation (4s cycle) for a living feel, smooth opacity transitions when filter changes, positioned behind calendar content for depth without distraction. Full light mode support with reduced intensity. Full prefers-reduced-motion support (no animation, static glow). Provides persistent visual feedback about the active filter state — users can instantly see at a glance which filter is applied. Common pattern in premium dashboards where filter state is reinforced through ambient visual cues.

### 2026-03-07 — 09:10
- **Premium gradient text selection** - Custom text selection highlighting with brand-matched gradient colors (blue → purple → pink). Features: subtle text glow effect on selected text for premium feel, theme-aware colors (softer, darker text in light mode for readability), contextual selection colors (green tint for success/beat text, red tint for danger/miss text). Respects the app's visual language throughout all user interactions. Common pattern in premium web apps that extends brand polish to every detail.
- **Enhanced gradient scrollbar** - Premium scrollbar styling with animated gradient thumb. Features: 8px width with rounded corners, gradient fill matching brand palette (blue → purple → pink), smooth hover transitions with glow effect, active state with increased opacity, full light mode support with softer colors, Firefox support via scrollbar-color property. Extends the premium feel to every interaction point. Subtle but noticeable polish that elevates the overall experience.

### 2026-03-07 — 11:20
- **Contextual hover shadow tinting** - Card shadows now get subtle color tints matching their result status. Features: beat cards get green-tinted multi-layer shadows (8px/4px/30px spreads) on hover, miss cards get red-tinted shadows, pending cards with odds gauge get amber-tinted shadows. Enhanced rotating gradient borders also match result colors (green conic gradient for beats, red for misses, amber for pending). Light mode support with softer shadow intensities (15% vs 20% opacity). Full prefers-reduced-motion support (shadows disabled). 2025 UI trend: cohesive visual feedback where shadows reinforce the semantic meaning of content colors. Creates unified visual language where every element of a card (badge, border, shadow) communicates the same result status. CSS-only using `:has()` selector for zero JS overhead.

### 2026-03-07 — 15:24
- **Scroll-velocity reactive ticker ribbon** - Ticker ribbon now responds to scroll speed, accelerating when the user scrolls faster for a dynamic "alive" feel. Features: useScrollVelocity hook with spring-smoothed velocity tracking using physics-based animation, configurable sensitivity and max/min multipliers, subtle border glow when velocity is boosted, motion blur effect at high velocities for sense of speed, ScrollVelocityProvider context for sharing velocity state across components, CSS custom property (--scroll-velocity) for styling hooks. Full light mode support with softer glow intensities. Full prefers-reduced-motion support (effects disabled). Inspired by Linear.app and shadcn.io scroll-velocity patterns where kinetic scroll-driven interactions create immersive experiences. 2025 micro-interaction trend that makes the page feel responsive to user input.

### 2026-03-07 — 21:39
- **Dynamic favicon with notification badge** - DynamicFavicon component shows pending earnings count as a badge on the browser favicon. Features: canvas-based rendering that composites badge over base icon, animated pulse effect when count increases (bounce easing), gradient badge styling (red with white border highlight), count > 99 displays as "99+", EmojiFavicon fallback using SVG for simpler implementation, auto-restores original favicon on unmount, respects tab visibility for performance. Common pattern in trading apps (Robinhood, Bloomberg, TradingView) that helps users notice pending earnings even when tab is backgrounded. Complements DynamicTitle for full attention-grabbing tab presence.


### 2026-03-07 — 23:54
- **Animated count badges on filter chips** - New AnimatedFilterCount component adds bouncy spring animations to filter count badges when values change. Features: direction-aware animations (green glow ring + pulse for count increases, red for decreases), scale overshoot using spring-bouncy physics timing, variant colors matching filter semantics (beat=green, miss=red, pending=amber), subtle radial glow that expands and fades on change, aria-live for accessibility. Integrated into FilterChips component. Full light mode support with softer glow intensities. Full prefers-reduced-motion support (instant changes, no animation). Provides instant visual feedback when data updates (e.g., when a stock reports and pending count drops from 5→4, the badge pulses red). Classic pattern in trading dashboards where count changes deserve attention. Makes the filter chips feel alive and reactive to real-time data changes.


### 2026-03-08 — 04:05
- **Data freshness indicator** - New DataFreshnessIndicator component showing when earnings data was last refreshed. Features: animated pulsing dot with intensity based on freshness (faster pulse = more recent data), color transitions (green fresh → amber aging → red stale), SVG staleness ring that depletes over time, live relative time display ('Just now', '2m ago', etc.), refresh button with spinning animation, hover tooltip showing exact sync timestamp, compact variant for header placement, inline and mini dot variants for tight spaces. Integrated into main header next to MarketStatus (lg+ screens). Common pattern in Bloomberg Terminal, TradingView, and premium finance dashboards where data recency is critical information for traders. Full light/dark mode support with appropriate color adjustments. Full prefers-reduced-motion support (animations disabled).
